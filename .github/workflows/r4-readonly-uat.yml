name: R4 Read-Only External UAT

on:
  push:
    branches: [main]
    paths:
      - 'tests/r4_readonly_uat.py'
      - 'tests/r4_identity_uat.py'
      - '.github/workflows/r4-readonly-uat.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  uat:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Execute R4 read-only UAT
        run: python tests/r4_readonly_uat.py
      - name: Execute R4 identity UAT
        run: python tests/r4_identity_uat.py
